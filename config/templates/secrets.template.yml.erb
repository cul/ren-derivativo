development:
  secret_key_base: <%= SecureRandom.hex(64) %>
  session_store_key: _derivativo_development_session_key
test:
  secret_key_base: <%= SecureRandom.hex(64) %>
  session_store_key: _derivativo_test_session_key
